security:
  oauth2:
    client:
      client-id: web
      client-secret: webpass
      user-authorization-uri: http://localhost:8383/oauth/authorize
      access-token-uri: http://localhost:8383/oauth/token
    resource:
      token-info-uri: http://localhost:8383/oauth/check_token

server:
  servlet:
    session:
      cookie:
        name: KSESSIONID
