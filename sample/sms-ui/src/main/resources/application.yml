security:
  oauth2:
    client:
      client-id: mobile
      client-secret: pin
      user-authorization-uri: http://localhost:8980/oauth/authorize
      access-token-uri: http://localhost:8980/oauth/authorize
    resource:
      token-info-uri: http://localhost:8980/oauth/check_token

server:
  servlet:
    session:
      cookie:
        name: KSESSIONID
